language: python
python:
  - "3.6"
sudo: false
cache: pip
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - sphinx-build -nWT -b dummy doc _build/html
  - pytest
