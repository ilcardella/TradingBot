language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.8-dev"

services:
  - docker

install:
  - pip install --upgrade pip
  - pip install poetry
  - make install

script:
  - make format
  - make lint
  - make test
  - make docs
  - make build
  - make install-setup
  - make docker
